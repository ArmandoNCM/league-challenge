<resources>

    <style name="Theme.LeagueCodeChallenge" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Login screen: reference dimens for consistency with Compose login UI -->
    <style name="LoginCard">
        <item name="android:padding">@dimen/login_card_inner_padding</item>
    </style>
</resources>
